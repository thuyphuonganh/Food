<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return Admin extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('admins', function (Blueprint $table) {
            $table->id(); // id INT AUTO_INCREMENT PRIMARY KEY
            $table->string('name'); // name VARCHAR(255) NOT NULL
            $table->string('address')->nullable(); // address VARCHAR(255) NULL
            $table->string('email')->unique(); // email VARCHAR(255) UNIQUE
            $table->timestamp('email_verified_at')->nullable(); // email_verified_at TIMESTAMP NULL
            $table->string('password'); // password VARCHAR(255) NOT NULL
            $table->rememberToken(); // remember_token VARCHAR(100) NULL
            $table->timestamps(); // created_at và updated_at
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('admins');
    }
};
